#include <iostream>

int main()
{
    /*
    For loop: Useful when loop counter is needed
              iterating over containers
              iterating over known number of elements
    
    While loop: Useful when halting criteraion is different and can be evaluated
                Unknown number of elements.   
    
    */
    return 0;
}
