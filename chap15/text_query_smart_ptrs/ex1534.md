## Expression Evaluation:

```cpp
Query q = Query("fiery") & Query("bird") | Query("wind");
```

### (a) Objects Constructed

```
2 objects:
Query(wind)
    |--> Word_Query(wind)

2 objects:
Query(bird)
    |--> Word_Query(bird)

2 objects:
Query(fiery)
    |--> Word_Query(fiery)

3 objects (operator & execution & return)
Query(Base_Query ptr)
    |--> And_Query (+ BinaryQuery)
            |-->lhs = Query(fiery)
            |-->rhs = Query(bird)

3 objects (operator | execution & return)
Query(Base_Query ptr)
    |--> Or_Query (+ BinaryQuery)
            |-->lhs = Query(BaseQuery ptr) // And_Query
            |-->rhs = Query(wind)

1 object 
Query q = ... (synthesized copy constructor)
```

A total of **13 objects** were created for the expression.

### (b) `rep()` Calls

```Query q = Query("fiery") & Query("bird") | Query("wind");

Query.rep()     // original call
    |
    +--> Binary_Query.rep() // And
            |
            +--> lhs: Query.rep() --> Binary_Query.rep() // Or
            |                            |
            |                            +--> lhs: Query.rep() --> Word_Query.rep() // fiery
            |                            +--> rhs: Query.rep() --> Word_Query.rep() // bird
            |
            +--> rhs: Query.rep() --> Word_Query.rep() // wind
```

A total of **10 rep()** calls (including the original).

### (c) `eval()` Calls

```Query q = Query("fiery") & Query("bird") | Query("wind");

Query.eval()
    |
    +--> Or_Query.eval() // Right Or
            |
            +--> lhs: Query.eval() // the And-Result
            |       |
            |       +--> And_Query.eval() // Left And
            |                |
            |                +--> lhs: Query.eval() --> Word_Query.eval()
            |                +--> rhs. Query.eval() --> Word_Query.eval()
            |
            +--> Query.eval() --> Word_Query.eval()
```

A total of **10 eval()** calls (including the original).